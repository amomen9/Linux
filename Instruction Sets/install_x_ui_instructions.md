# Install and use `x-ui`

This follows the upstream install script.

## Steps

```bash
sudo apt update && sudo apt upgrade -y
sudo apt install -y curl socat

# Run installer
bash <(curl -Ls https://raw.githubusercontent.com/vaxilu/x-ui/master/install.sh)
```

Note: Review scripts before running in production environments.
