============================================================================
  SUPPORTED LINUX DISTRIBUTIONS
  (Auto-generated from manifest: 2026-07-04 20:17)
============================================================================

This file is generated by run_project.ps1 by scanning the per-distro install
descriptors in documents/B_applications.json.  Only families with at least
one app providing a native package name are listed as "supported."

The generated installer in "Execute on Linux!/" is UNIVERSAL: it detects the
running distribution FAMILY and CPU ARCHITECTURE at runtime and dispatches to
the correct package manager.  Distributions are grouped by their parent family,
so adding a derivative (e.g. Pop!_OS, AlmaLinux, Manjaro) needs no new code --
it is matched automatically via /etc/os-release ID_LIKE.

Application delivery is FLATPAK-FIRST (Flathub app ids are identical on every
distro and every architecture).  The native package manager is used only for
the base layer: installing Flatpak itself, CLI/developer tools, drivers and
system components.

----------------------------------------------------------------------------
  OVERVIEW  (ranked by overall popularity, most popular first)
----------------------------------------------------------------------------

  #  FAMILY (package manager)     BEST FOR
  -- ---------------------------- ------------------------------------------
  1  Debian / Ubuntu (apt)        General-purpose desktops & servers (safe default)
  2  RHEL / Fedora (dnf)          Enterprise servers & modern workstations
  3  Arch (pacman)                Power-user desktops & gaming (rolling release)
  4  openSUSE / SUSE (zypper)     Reliable workstations & servers (YaST, btrfs snapshots)
  5  Alpine (apk)                 Containers, servers & embedded (tiny, musl-based)
  6  Gentoo (emerge)              Source-based customization & performance tuning (advanced)
  7  Slackware (slackpkg)         UNIX-purist simplicity & long-term stability

----------------------------------------------------------------------------
  FAMILY            PACKAGE MANAGER   NATIVE APPS IN MANIFEST
----------------------------------------------------------------------------

  [ Debian / Ubuntu ] apt (apt-get)          98 app(s)
    * 25 app(s) flagged for review on this family
    Best for: General-purpose desktops & servers (safe default)
      - Linux Mint (Ubuntu)
      - Ubuntu
      - Debian
      - Zorin OS
      - Kubuntu
      - Winux 11
    Also auto-detected: Pop!_OS, elementary OS, and other Ubuntu/Debian derivatives.

  [ RHEL / Fedora ] dnf                    82 app(s)
    * 20 app(s) flagged for review on this family
    Best for: Enterprise servers & modern workstations
      - Fedora
      - Rocky Linux
      - Red Hat Enterprise Linux (RHEL)
      - Oracle Linux
    Also auto-detected: AlmaLinux, CentOS Stream.
    Note: RHEL rebuilds may need EPEL enabled for a few packages.

  [ Arch ] pacman                 73 app(s)
    * 19 app(s) flagged for review on this family
    Best for: Power-user desktops & gaming (rolling release)
      - Arch Linux
    Also auto-detected: Manjaro, EndeavourOS, SteamOS.

  [ openSUSE / SUSE ] zypper                 50 app(s)
    * 9 app(s) flagged for review on this family
    Best for: Reliable workstations & servers (YaST, btrfs snapshots)
      - openSUSE (Leap / Tumbleweed)
    Also auto-detected: SLE (SLES / SLED).

  [ Alpine ] apk                    Flatpak-first (native fallback: Debian names)
    Best for: Containers, servers & embedded (tiny, musl-based)
      - Alpine Linux
    Also auto-detected: postmarketOS and other apk-based systems.
    Note: Server/container-oriented and musl-libc based (not glibc). Flatpak apps work (they bundle their own glibc runtime), but glibc-only direct .deb/.rpm downloads and Wine are unreliable here. Usable as a workstation mainly via postmarketOS.

  [ Gentoo ] emerge (portage)       Flatpak-first (native fallback: Debian names)
    Best for: Source-based customization & performance tuning (advanced)
      - Gentoo
      - Funtoo
    Note: Source-based, but GUI apps install Flatpak-first (no long compiles); the native fallback reuses the Debian package names. Full workstation-capable. Keep the Portage tree fresh with "emerge --sync" / emerge-webrsync.

  [ Slackware ] slackpkg               Flatpak-first (native fallback: Debian names)
    Best for: UNIX-purist simplicity & long-term stability
      - Slackware
    Note: The base tools do no automatic dependency resolution, so apps install Flatpak-first (self-contained); the native fallback reuses the Debian names and some extras may need SlackBuilds.org. Workstation-capable.

----------------------------------------------------------------------------
  CPU ARCHITECTURES
----------------------------------------------------------------------------

  x86_64  (amd64)   - fully supported
  aarch64 (arm64)   - supported; Flatpak/Flathub and native repos serve the
                      matching build automatically
  ARCHITECTURE CONSTRAINT: 2 app(s) explicitly constrain to: x86_64, x86_64 aarch64
  Windows-app compatibility (Wine via Bottles) is x86_64-only and is skipped
  on ARM with a message.

  Notes:
    - Flatpak and native package installs are architecture-transparent.
    - Only direct downloads (.deb/.rpm/AppImage/GitHub-release assets) need an
      architecture-specific URL; the installer picks it from `uname -m`.
    - Windows-app compatibility (Wine via Bottles) is x86_64-only and is skipped
      on ARM with a message.

----------------------------------------------------------------------------
  COVERAGE NOTES
----------------------------------------------------------------------------

  Of 154 "must-include" applications in the manifest:
    -   98 have apt entries
    -   82 have dnf entries
    -   73 have pacman entries
    -   50 have zypper entries
    -   73 have Flatpak IDs (distro-agnostic)

  Gentoo (emerge), Slackware (slackpkg) and Alpine (apk) are also supported: apps
  install Flatpak-first and the native fallback reuses the Debian package names.
  Add per-family names under install.native.{emerge,slackpkg,apk} to curate them.
  Alpine is musl-based and server/container-oriented (glibc direct-downloads and
  Wine are unreliable there); it is a workstation mainly via postmarketOS.

  The Debian / Ubuntu (apt) path has the most native entries and is the
  most heavily tested.  Native package names for the other families are
  best-effort based on what has been curated in the manifest.  Entries
  flagged with 'review: true' need verification on a real system of that
  family.

============================================================================